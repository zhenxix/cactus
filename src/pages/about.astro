---
import PageLayout from "@/layouts/Base.astro";
import SocialList from "@/components/SocialList.astro";
export const prerender = true;

const meta = {
  description: "仙人掌主题",
  title: "关于",
};
---

<PageLayout meta={meta}>
  <h1 class="title mb-8">关于</h1>

  <div class="prose prose-sm prose-cactus max-w-none">
    <p class="gradient-1">大家好！欢迎来到我的博客！我是Zhen XI，一个热爱生活、喜欢分享的开发爱好者。在这里，我希望通过文字记录我的所思所想，与志同道合的朋友们交流和成长。</p>
    <p class="gradient-2">开这个博客的初衷很简单：我想有一个属于自己的小角落，记录生活的点滴，同时也希望我的内容能给读者带来一些启发或欢乐。如果我的文字能让你会心一笑，或者引发你的共鸣，那将是我的荣幸！</p>
    <p class="gradient-3">如果你有任何想法或者建议，也可以通过zhenxi@linux.do与我联系，我会认真倾听每一条反馈。</p>
    <p class="gradient-4">未来的日子里，我会持续更新博客内容，分享更多有趣的故事和有价值的信息。希望你能常来坐坐，我们一起在文字的世界里探索和成长！</p>
    <p class="gradient-5">谢谢你的到来！</p>
  </div>

  <div>
    <SocialList />
  </div>

  <style>
    :root {
      --color-1: #FF6B6B;   /* 珊瑚红 */
      --color-2: #4ECDC4;   /* 青绿 */
      --color-3: #FFB085;   /* 浅橙 */
      --color-4: #6B5B95;   /* 紫罗兰 */
      --color-5: #88B04B;   /* 橄榄绿 */
      --color-6: #FFD6A5;   /* 橘粉色 */
      --color-7: #86A2B9;   /* 银蓝 */
      --color-background: #f9f9f9;
    }

    /* 渐变色类 */
    .gradient-1 {
      background: linear-gradient(90deg, var(--color-1), var(--color-2), var(--color-3), var(--color-4), var(--color-5));
      -webkit-background-clip: text;
      color: transparent;
      font-weight: bold;
      margin-top: 1rem;
    }

    .gradient-2 {
      background: linear-gradient(90deg, var(--color-2), var(--color-3), var(--color-4), var(--color-5), var(--color-6));
      -webkit-background-clip: text;
      color: transparent;
      font-weight: bold;
      margin-top: 1rem;
    }

    .gradient-3 {
      background: linear-gradient(90deg, var(--color-3), var(--color-4), var(--color-5), var(--color-6), var(--color-7));
      -webkit-background-clip: text;
      color: transparent;
      font-weight: bold;
      margin-top: 1rem;
    }

    .gradient-4 {
      background: linear-gradient(90deg, var(--color-4), var(--color-5), var(--color-6), var(--color-7), var(--color-1));
      -webkit-background-clip: text;
      color: transparent;
      font-weight: bold;
      margin-top: 1rem;
    }

    .gradient-5 {
      background: linear-gradient(90deg, var(--color-5), var(--color-6), var(--color-7), var(--color-1), var(--color-2));
      -webkit-background-clip: text;
      color: transparent;
      font-weight: bold;
      margin-top: 1rem;
    }

    /* 悬停交互 */
    .gradient-1:hover {
      background: linear-gradient(90deg, var(--color-1), var(--color-2), var(--color-3), var(--color-4), var(--color-5));
      -webkit-background-clip: text;
      color: transparent;
      transition: background 0.3s ease;
    }

    .gradient-2:hover {
      background: linear-gradient(90deg, var(--color-2), var(--color-3), var(--color-4), var(--color-5), var(--color-6));
      -webkit-background-clip: text;
      color: transparent;
      transition: background 0.3s ease;
    }

    .gradient-3:hover {
      background: linear-gradient(90deg, var(--color-3), var(--color-4), var(--color-5), var(--color-6), var(--color-7));
      -webkit-background-clip: text;
      color: transparent;
      transition: background 0.3s ease;
    }

    .gradient-4:hover {
      background: linear-gradient(90deg, var(--color-4), var(--color-5), var(--color-6), var(--color-7), var(--color-1));
      -webkit-background-clip: text;
      color: transparent;
      transition: background 0.3s ease;
    }

    .gradient-5:hover {
      background: linear-gradient(90deg, var(--color-5), var(--color-6), var(--color-7), var(--color-1), var(--color-2));
      -webkit-background-clip: text;
      color: transparent;
      transition: background 0.3s ease;
    }

    /* 响应式设计 */
    .prose-cactus {
      background-color: var(--color-background);
      padding: 1.5rem;
      border-radius: 10px;
    }

    @media (max-width: 600px) {
      .prose-cactus p {
        font-size: 1rem;
        line-height: 1.6;
      }

      .prose-cactus h1 {
        font-size: 1.5rem;
      }
    }
  </style>
</PageLayout>
